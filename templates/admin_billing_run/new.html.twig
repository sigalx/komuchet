{% extends 'admin/base.html.twig' %}

{% block title %}Новый Расчет{% endblock %}

{% block admin_body %}
    <div class="d-flex flex-wrap align-items-start justify-content-between gap-3 mb-4">
        <div>
            <h1 class="h3 mb-1">Новый расчет</h1>
            <p class="text-secondary mb-0">Черновик расчета фиксирует тип и расчетный период.</p>
        </div>
        <a class="btn btn-outline-secondary" href="{{ path('app_admin_billing_run_index') }}">К списку</a>
    </div>

    <section class="card">
        <div class="card-body">
            {{ include('admin_billing_run/_form.html.twig') }}
        </div>
    </section>
{% endblock %}
