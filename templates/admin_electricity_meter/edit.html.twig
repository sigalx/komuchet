{% extends 'admin/base.html.twig' %}

{% block title %}Электросчетчик {{ electricity_meter.account.number }}{% endblock %}

{% block admin_body %}
    <div class="d-flex flex-wrap align-items-start justify-content-between gap-3 mb-4">
        <div>
            <h1 class="h3 mb-1">Электросчетчик участка {{ electricity_meter.account.number }}</h1>
            <p class="text-secondary mb-0">Участок и регистры счетчика после создания не редактируются.</p>
        </div>
        {{ include('admin_electricity_meter/_delete_form.html.twig') }}
    </div>

    <section class="card">
        <div class="card-body">
            {{ include('admin_electricity_meter/_form.html.twig', {'button_label': 'Сохранить'}) }}
        </div>
    </section>
{% endblock %}
