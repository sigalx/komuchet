<form method="post" action="{{ path('app_admin_electricity_tariff_profile_delete', {'uuid': electricity_tariff_profile.uuid}) }}" onsubmit="return confirm('Удалить тарифный профиль {{ electricity_tariff_profile.name|e('js') }}?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ electricity_tariff_profile.uuid) }}">
    <button class="btn btn-outline-danger">Удалить</button>
</form>
