{% extends 'admin/base.html.twig' %}

{% block title %}Участок {{ account.number }}{% endblock %}

{% block admin_body %}
    <div class="d-flex flex-wrap align-items-start justify-content-between gap-3 mb-4">
        <div>
            <h1 class="h3 mb-1">Участок {{ account.number }}</h1>
            <p class="text-secondary mb-0">Изменение номера и служебной заметки.</p>
        </div>
        {{ include('admin_account/_delete_form.html.twig') }}
    </div>

    <section class="card">
        <div class="card-body">
            {{ include('admin_account/_form.html.twig', {'button_label': 'Сохранить'}) }}
        </div>
    </section>
{% endblock %}
